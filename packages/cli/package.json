{
  "name": "@xwartz/test-token-ui",
  "version": "0.1.0",
  "description": "CLI for adding Token UI components to React projects.",
  "type": "module",
  "bin": {
    "test-token-ui": "./dist/index.js"
  },
  "files": [
    "dist",
    "registry"
  ],
  "scripts": {
    "sync:registry": "tsx scripts/sync-registry.ts",
    "prebuild": "pnpm sync:registry",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "dev": "tsx src/index.ts"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^22.10.7",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3"
  }
}
